# Copy to .env and fill these values to enable real SMTP sending
# Example for Gmail (use an App Password if you have 2FA enabled):
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# EMAIL_FROM=Your Name <your-email@gmail.com>

# By default, if these are not provided, the server will use a JSON transport (no real emails sent).
